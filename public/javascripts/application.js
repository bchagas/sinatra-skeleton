(function(){
  window.appname = jQuery.extend({ // Change appname to your application name.
    init: function(){
      // Call to your functions here!
    },

    // Create new functions... :D
  });
$().ready(appname.init);
}).call(this);
